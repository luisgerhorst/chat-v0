The javascript sends the message (using Ajax) to save.php that writes it (including current time and HTML Tags) into chatlog.txt. javascript.js loads the content of chatlog.txt regularly into the div #chat.